# Development
NODE_ENV="development"

# Database (Transaction Pooler in Supabase)
POSTGRES_URL=postgresql://***

# App Configuration
NEXT_PUBLIC_APP_NAME="Escrowzy"
NEXT_PUBLIC_APP_DESCRIPTION="A gamified decentralized escrow and P2P trading platform on Etherlink where users battle for trading fee discounts, earn achievement NFTs, and level up through every transaction."
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Authentication (openssl rand -base64 32)
AUTH_SECRET=***

# Cron Jobs Password to prevent unauthorized access (optional)
CRON_SECRET=

# Wallet Provider Configuration (thirdweb or rainbow-kit)
NEXT_PUBLIC_WALLET_PROVIDER=rainbow-kit

# Thirdweb Client ID (get from https://thirdweb.com/dashboard)
NEXT_PUBLIC_THIRDWEB_CLIENT_ID=YOUR_THIRDWEB_CLIENT_ID

# WalletConnect Project ID (get from https://cloud.walletconnect.com/) - used by RainbowKit
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=YOUR_PROJECT_ID

# Email Service (Resend) - Get from https://resend.com/
# Add and verify your domain on https://resend.com/domains
RESEND_API_KEY=your-resend-api-key
FROM_EMAIL=noreply@yourdomain.com

# Admin Contact Email
ADMIN_EMAIL=admin@yourdomain.com

# Blockchain Configuration
# Path to blockchain configuration YAML file
BLOCKCHAIN_CONFIG_PATH=./config/blockchains.yaml

# Admin Configuration
ADMIN_ADDRESS=0x0000000000000000000000000000000000000000
ADMIN_PRIVATE_KEY=your-private-key-here

# API Keys
COINGECKO_API_KEY=your-coingecko-api-key

# Pusher Configuration (for real-time chat)
# https://dashboard.pusher.com/apps -> Goto your app -> App keys
PUSHER_APP_ID=your-pusher-app-id
NEXT_PUBLIC_PUSHER_KEY=your-pusher-key
PUSHER_SECRET=your-pusher-secret
NEXT_PUBLIC_PUSHER_CLUSTER=your-pusher-cluster
